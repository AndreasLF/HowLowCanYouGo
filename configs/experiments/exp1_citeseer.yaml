dataset_path: './data/adj_matrices/Citeseer.pt'
dataset_ref: 'PyTorch-Geometric/Planetoid/Citeseer'
model_types: ['L2', 'PCA']
loss_types: ['poisson', 'hinge', 'logistic']
num_epochs: 100_000
batch_size_percentage: 1.0
model_init: 'random'
lr: 1.0
early_stop_patience: 200 # Number of consecutive epochs without improvement after which early stopping will be applied
rank_range:
  min: 1
  max: 64