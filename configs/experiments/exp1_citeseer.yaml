dataset_path: './data/adj_matrices/Citeseer.pt'
dataset_ref: 'PyTorch-Geometric/Planetoid/Cora'
model_types: ['L2', 'PCA']
loss_types: ['logistic', 'hinge']
num_epochs: 50_000


model_init: 'random'
lr: 1.0
early_stop_patience: 500 # Number of consecutive epochs without improvement after which early stopping will be applied
rank_range:
  min: 1
  max: 50